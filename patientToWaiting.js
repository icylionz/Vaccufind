function addPatientToWaitingList(patientAdded) {
	var waitingIndex;
	var patientWaiting;
	var waitingIndex = 0;
	var s = new FileReader();

	while (s != s.AtEndOfStream) {
	//read indexth record of file database
		if (patientWaiting.tag > patientAdded.tag) {
			//insert between indexth location and waitingindex - 1 location
		}
	}

	//insert patient at end of file
		
}